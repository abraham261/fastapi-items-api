services:
  - type: web
    name: fastapi-items-api
    python:
      version: 3.11.11  # À adapter à votre version
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT